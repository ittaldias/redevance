name: RDVC application

on:
  push:
    branches: [ main ]
    paths:
        - 'src/**'
        - 'test2/**'
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'  # Adjust to your Python version

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

  lecture_stan:
    runs-on: ubuntu-latest
    needs: build
    outputs:
      special_value: ${{ steps.set_value.outputs.result }}  # Job-level output
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
             python -m pip install --upgrade pip
             pip install -r requirements.txt

      - name: Generate Value
        id: set_value  # Important: set an ID for this step to refer to its outputs
        run: echo "::set-output name=result::output"  # Set 'glmf' as output

  traitement_preliminaire:
    runs-on: ubuntu-latest
    needs: lecture_stan
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
             python -m pip install --upgrade pip
             pip install -r requirements.txt

      - name: Use the Output from Previous Job
        run: echo "The output value is ${{ needs.lecture_stan.outputs.special_value }}"  # Use the output from lecture_stan job
      - name: Test print
        run: print(result.head)
  traitement_pln_utile:
    runs-on: ubuntu-latest
    needs: traitement_preliminaire
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'  # Adjust to your Python version

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt 
    - name: Run tests
      run: pytest -v test2/test_algo_b_2_4_bis_traitement_unitaire.py -s
